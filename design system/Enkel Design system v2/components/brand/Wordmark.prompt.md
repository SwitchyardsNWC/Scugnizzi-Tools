Lowercase brand wordmark with trailing underscore — use wherever a logo would go (no mark exists).
```jsx
<Wordmark name="art-ventory" size={21} href="/" />
```
- `size`: 21 (sidebar), 30, 62 (display). Never uppercase; never drop the underscore.
