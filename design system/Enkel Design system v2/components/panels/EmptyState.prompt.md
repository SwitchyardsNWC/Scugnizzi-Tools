Placeholder that looks deliberate, not broken.
```jsx
<EmptyState ratio="16 / 9" what="Floor plan goes here" action={<Button size="sm" variant="outline">Add plan</Button>} />
<EmptyState compact what="No photos yet" action={<TextLink arrow>Upload</TextLink>} />
```
