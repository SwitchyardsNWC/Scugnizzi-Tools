Status / category chip — square, hairline, black or fossil. Never coloured.
```jsx
<Tag>In storage</Tag> <Tag tone="muted">On loan</Tag> <Tag tone="filled">Sold</Tag>
```
- 24px tall, padding 3/1 — text reads optically centred; don't re-centre it with flex.
- Sized to its text (`max-content`) and clipped at 100% of its column: it never stretches to fill a grid cell, never pushes past one.
